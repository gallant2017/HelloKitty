package com.gallant.dubbo.provider.api.dao;

import com.gallant.dubbo.provider.api.entity.UserDto;


import java.util.List;

//@Mapper
public interface UserDao {

//    @Select("select userid,username,address from users")
//    List<UserDto> getUserList();
//
//    @Select("insert into users(username,address,createdon) values(#{username},#{address},now())")
//    @Options(useGeneratedKeys = true, keyProperty = "userid", keyColumn = "userid")
//    Integer AddUser(UserDto user);
}
